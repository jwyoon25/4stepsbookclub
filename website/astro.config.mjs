import { defineConfig } from "astro/config";

export default defineConfig({
  // Absolute base for canonical and Open Graph URLs. Link previews break if
  // this does not match the domain the site is actually served from.
  site: "https://4stepsbookclub.com"
});
